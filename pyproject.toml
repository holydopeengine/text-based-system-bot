[project]
name = "text-based-system-bot"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "holydopeengine", email = "109796839+holydopeengine@users.noreply.github.com" }
]
requires-python = ">=3.13"
dependencies = [
    "python-dotenv>=1.2.3",
    "python-telegram-bot>=22.8",
]

[project.scripts]
text-based-system-bot = "text_based_system_bot:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
